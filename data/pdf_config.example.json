{
  "cloud_token": "在此填入你的 MinerU Precision Token（先在 https://mineru.net 免费注册，再到 https://mineru.net/apiManage/token 领取/重置）",
  "token_created_at": "YYYY-MM-DD",
  "token_validity_days": 90,
  "note": "用法：把本文件复制为 pdf_config.json 并填入真实 Token 与签发日期。pdf_config.json 已被 .gitignore 排除，不会进入 git，避免泄露。注意：用云端加速还需在 pdf_worker/.venv 里装 mineru-open-sdk（它与本地引擎的 mineru 是两个独立的包）。"
}
